### git

alias gcm='git checkout master && git pull'
alias g='git'

### FAKE (.NET)
case "$OSTYPE" in
  (linux*)
    alias fake='./fake.sh'
  ;;
  (cygwin*)
    alias fake='./fake.bat'
  ;;
esac

alias vim='nvim'
